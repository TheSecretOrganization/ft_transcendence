Table users {
  id integer [primary key]
  username varchar
  created_at timestamp
}

Table password {
  user integer [primary key, ref: <users.id]
  password string
}

Table ft {
  user integer [primary key, ref: <users.id]
  ft_id integer [unique]
  token string [note: 'necessary?']
}
